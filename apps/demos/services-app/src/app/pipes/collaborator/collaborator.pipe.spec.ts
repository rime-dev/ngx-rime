import {CollaboratorPipe} from './collaborator.pipe';

describe('CollaboratorPipe', () => {
  it('create an instance', () => {
    const pipe = new CollaboratorPipe();
    expect(pipe).toBeTruthy();
  });
});
